# Confirm if redis cache is working
# Solve the pagination issue. Where only 10 pages are parsed instead of 100s in the KAMIS website.
# Update doc
# Make API not reliant to one AI service provider. (Anthropic API KEY, should accept from other vendors too. Esp Open Source ones)
